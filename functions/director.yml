version: 1.0
provider:
  name: openfaas
  gateway: http://localhost:8080
functions:
  director:
    lang: python3
    handler: ./director
    image: milosveljkovic97/director:0.1.1
    environment:
      write_debug: true

